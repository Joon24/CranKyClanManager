[build]
builder = "DOCKERFILE"
dockerfilePath = "bot/Dockerfile"
watchPatterns = ["bot/**", "package.json", "package-lock.json"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
